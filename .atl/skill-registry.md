# Skill Registry — AIClient-2-API

> Auto-generated by SDD init. Last updated: 2026-04-07

## Project Context

- **Stack**: Node.js 20+, ESM modules, async generators
- **Testing**: Jest + Supertest (tests/**/*.test.js)
- **Architecture**: Strategy + Adapter patterns, modular providers
- **HTTP**: Native http module (no Express)
- **Streaming**: SSE via async generators (yield*)

## Available Skills

### User Skills (from ~/.config/opencode/skills/)

| Skill | Trigger | Relevance |
|-------|---------|-----------|
| branch-pr | Creating pull requests | ✅ Relevant |
| issue-creation | Creating GitHub issues | ✅ Relevant |
| skill-creator | Creating new AI skills | ✅ Relevant |
| go-testing | Go tests, Bubbletea TUI | ❌ Not relevant (Node.js project) |
| judgment-day | Adversarial review | ⚠️ Optional |

### SDD Skills (auto-loaded by orchestrator)

- sdd-init, sdd-explore, sdd-propose, sdd-spec, sdd-design, sdd-tasks, sdd-apply, sdd-verify, sdd-archive

## Project-specific Conventions

### Code Patterns

```javascript
// Adapter pattern - all providers extend ApiServiceAdapter
class ProviderApiServiceAdapter extends ApiServiceAdapter {
  async generateContent(model, requestBody) { ... }
  async *generateContentStream(model, requestBody) { yield* ... }
  async refreshToken() { ... }
}

// Converter pattern - protocol conversion
class ProviderConverter {
  toOpenAIRequest(request) { ... }
  toOpenAIResponse(response) { ... }
  toOpenAIStreamChunk(chunk) { ... }
}
```

### File Structure

```
src/
├── core/           # master.js, config-manager.js, plugin-manager.js
├── providers/      # adapter.js, gemini/, openai/, claude/, grok/
├── converters/     # ConverterFactory.js, strategies/
├── handlers/       # request-handler.js
├── services/       # api-server.js, api-manager.js
├── auth/           # oauth handlers per provider
└── utils/          # logger.js, common.js, constants.js
```

### Testing Conventions

- Unit tests: `tests/**/*.unit.test.js`
- Integration tests: `tests/**/*.test.js`
- Run: `npm test` or `npm run test:coverage`

## Compact Rules (for sub-agent injection)

### Node.js/ESM Rules
```markdown
- Use ESM imports: `import { foo } from './bar.js'`
- Async generators for streaming: `async *stream() { yield* ... }`
- No Express - use native http module
- All providers implement ApiServiceAdapter interface
```

### Architecture Rules
```markdown
- Strategy pattern for converters
- Adapter pattern for providers
- Plugin system for middleware/auth
- Registry-based provider lookup
```